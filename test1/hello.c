#include <stdio.h>

/**
 * argc : the number of command line arguments
 * argv : array of parameters, each of which is a string (char *)
*/
int main(int argc, char *argv[])
{
    // int i;
    // printf("%d\n", argc);
    // for (i = 0; i < argc; i++)
    //     printf("%s\n", argv[i]);
    printf("hello world\n");
    return 0;
}